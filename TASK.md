# 项目实施待办清单 (TODO List)

> 本文件按实施阶段拆分了三个任务的具体工作项，每完成一项请勾选 `[x]`。
> 建议按顺序推进，确保模型、代码、图表和分析完整闭环。

## 环境与准备工作

- [x] 1\. 创建并激活 Python 虚拟环境 (`.venv`)，确保版本为 3.13。

- [x] 2\. 安装项目依赖 (`numpy`, `matplotlib`)。

- [x] 3\. 在项目根目录运行 `python main.py` 测试环境是否正常 (无报错)。

- [ ] 4\. 确认 `data/` 和 `figures/` 文件夹已创建 (若无，手动创建)。

- [ ] 5\. 准备任务三所需的样本数据 (放入 `data/sample_data.csv`)。

## 任务一：迭代法解线性方程组

### 2.1 数学模型层

- [x] 6\. 定义实际或模拟问题，写出线性方程组 $Ax = b$。

- [x] 7\. 明确矩阵 $A$、未知量 $x$、右端项 $b$ 的具体数值与物理 / 工程含义。

- [x] 8\. 说明建模假设 (如矩阵是否对角占优、是否稀疏等)。

### 2.2 计算实现层

- [x] 9\. 编写 `tasks/iterative/core.py`，实现 **Jacobi 迭代法**。

- [x] 10\. (可选) 实现 **Gauss-Seidel 迭代法** 以作对比。

- [x] 11\. 设置初始解 $x^{(0)}$、误差阈值 (如 $10^{-8}$) 和最大迭代次数。

- [x] 12\. 记录每次迭代的残差 (或相对误差)，并返回收敛后的解及迭代历史。

- [x] 13\. 使用 `numpy.linalg.solve` 或 `scipy.linalg.solve` 计算直接解作为参考。

### 2.3 可视化验证层

- [x] 14\. 编写 `tasks/iterative/visualize.py`，生成 **矩阵结构热力图** (展示元素分布或稀疏性)。

- [x] 15\. 绘制 **迭代误差随步数变化曲线** (对数坐标，对比不同方法)。

- [x] 16\. 绘制 **数值解与参考解对比图** (散点图或折线图)。

- [x] 17\. 保存图表至 `figures/` 目录 (命名规范，如 `task1_heatmap.png`)。

### 2.4 结果分析

- [ ] 18\. 分析迭代是否收敛，收敛速度如何。

- [ ] 19\. 比较 Jacobi 与 Gauss-Seidel 的迭代次数和误差。

- [ ] 20\. 讨论矩阵性质 (如谱半径、条件数) 对迭代效果的影响。

## 任务二：三控开关的设计与实现

### 3.1 状态与逻辑建模

- [x] 21\. 定义三个开关 $S_1, S_2, S_3$ 及其取值 (0 或 1)。

- [x] 22\. 枚举全部 8 种输入状态，列出真值表 (含输出灯状态 $L$)。

- [x] 23\. 根据真值表推导最简布尔表达式 (如 $L = S_1 \oplus S_2 \oplus S_3$)。

### 3.2 计算实现层

- [x] 24\. 编写 `tasks/three_switch/core.py`，实现真值表生成与布尔表达式计算。

- [x] 25\. 实现状态切换验证函数：给定任一状态和要切换的开关编号，检查输出是否正确翻转。

- [x] 26\. 程序能够接受任意输入组合并输出灯的状态。

### 3.3 可视化验证层

- [ ] 27\. 编写 `tasks/three_switch/visualize.py`，生成 **状态空间图** (8 个节点及其转移关系)。

- [ ] 28\. 生成 **真值表颜色矩阵** (用红 / 绿或 0/1 色块表示输出)。

- [x] 29\. (可选) 生成逻辑门结构图或布尔表达式树。

- [x] 30\. 生成 **状态切换路径图** (展示单开关切换时输出变化的轨迹)。

### 3.4 结果分析

- [ ] 31\. 验证程序输出是否与真值表完全一致。

- [ ] 32\. 确认任意一个开关变化时，灯状态均切换 (满足设计要求)。

- [ ] 33\. 讨论布尔表达式的简洁性及逻辑实现的可靠性。

## 任务三：采用 Bootstrap 方法解决估计问题

### 4.1 估计问题与数据说明

- [x] 34\. 明确估计问题 (如估计总体均值、中位数或某种比例)。

- [x] 35\. 载入 `data/sample_data.csv`，描述样本来源、样本量及基本统计特征。

- [x] 36\. 明确目标统计量和估计假设 (如独立同分布)。

### 4.2 Bootstrap 方法实现

- [x] 37\. 编写 `tasks/bootstrap/core.py`，实现有放回重采样函数。

- [x] 38\. 设置重采样次数 $B = 1000$(可配置)，每次采样大小等于原样本量。

- [x] 39\. 计算每个 Bootstrap 样本的统计量，存储为数组。

- [x] 40\. 计算点估计 (原始样本统计量) 和百分位数置信区间 (如 95%)。

- [x] 41\. (可选) 尝试不同 $B$ 值 (如 500、2000) 对比稳定性。

### 4.3 可视化验证层

- [ ] 42\. 编写 `tasks/bootstrap/visualize.py`，生成 **原始样本分布图** (直方图 + 箱线图)。

- [ ] 43\. 绘制 **Bootstrap 统计量分布直方图 / 密度曲线**。

- [ ] 44\. 绘制 **置信区间示意图** (标出点估计、下限、上限)。

- [ ] 45\. (可选) 绘制 **统计量随重采样次数变化的演化曲线**。

### 4.4 结果分析

- [ ] 46\. 解释点估计和置信区间的实际含义。

- [ ] 47\. 评估估计分布的稳定性 (如标准误、偏度)。

- [ ] 48\. 讨论 Bootstrap 方法的局限性 (如对原始分布依赖性、小样本偏差)。

## 综合讨论与总结

- [ ] 49\. 从工程建模、数学方法、计算实现、可视化表达四个角度，总结三个任务之间的共性与联系 (写入报告)。

- [ ] 50\. 总结本次实践的收获、遇到的困难及解决办法。

- [ ] 51\. 提出可进一步改进的方向 (如算法优化、交互式可视化等)。

## 报告与过程文档

- [ ] 52\. 根据模板填写 `reports/实践报告.md`：
  - [ ] 52.1 基本信息 (项目名称、组号、成员、日期)。

  - [ ] 52.2 摘要 (200-300 字)。

  - [ ] 52.3 任务一至任务三的完整章节 (模型、实现、可视化、分析)。

  - [ ] 52.4 综合讨论、总结与改进、参考资料、附录。

- [ ] 53\. 根据模板填写 `reports/实践过程.md`：
  - [ ] 53.1 基本信息。

  - [ ] 53.2 资料查阅与实践过程 (时间、主题、来源、收获)。

  - [ ] 53.3 问题记录与解决 (时间、问题、过程、结果与反思)。

  - [ ] 53.4 分工与协作记录 (成员、负责内容、协作情况、完成情况)。

  - [ ] 53.5 阶段性推进记录 (时间、完成内容、阶段成果、下一步计划)。

  - [ ] 53.6 总结与反思。

- [ ] 54\. 检查报告中所有图表是否插入了正确的图片路径，并附有编号、标题和文字解释。

- [ ] 55\. 检查参考文献、AI 工具使用情况是否已如实声明。

## 最终提交与检查

- [ ] 56\. 确认所有代码可运行，且 `main.py` 能一键生成全部图表。

- [ ] 57\. 确认 `figures/` 中包含了所有报告引用的图片。

- [ ] 58\. 整理文件夹，按规范命名压缩包：`第x组-2026数学与思维实践项目.zip`。

- [ ] 59\. 内部结构符合要求：`实践报告/`、`实践过程/`、`源代码与数据/`、`可视化结果/`(或按实际要求)。

- [ ] 60\. 最终提交前通读报告，修正错别字、格式和逻辑问题。

> **提示**：每完成一个小项，将 `- [ ]` 改为 `- [x]`，方便直观查看整体进度。
